import React from 'react'

export default function JSX() {
  return (
    <div>
      <h3>JSX (JavaScript XML)</h3>
      <ul>
        <li>It helps use to use HTML and JS together.</li>
        <li>We can use React without JSX, but its very complicated.</li>
        <li>Babel compiler is used to Convert JSX into browser understanding JS. As browser can't understanding JSX directly.</li>
      </ul>
    </div>
  )
}
