import React from 'react'

export default function PackageJson() {
  return (
    <div>
      <h3>Package.json</h3>
      <p>It kept detail of our project, like: Libraries, dependencies that we have used in our project and their version</p>
      <strong>To install an package:</strong><br />
      <code>npm install packageName</code>
    </div>
  )
}
